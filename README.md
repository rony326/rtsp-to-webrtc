# rtsp-to-webrtc
WebRTC-Variante Standby via HLS, Live via go2rtc → WebRTC direkt im Browser. Latenz &lt;500ms. Benötigt zweiten Container (go2rtc). Empfohlen für zeitkritische Anwendungen wie Türklingeln.
